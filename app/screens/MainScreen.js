const MainScreen = ({ navigation }) => {
  return (
    <View style={styles.screen}>
      <Text>1. Capture a full screenshot of the suspicious message</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  screen: {
    flex: 1,
    backgroundColor: 'white',
  },
});
